import { Bar, BarChart, CartesianGrid, Cell, Line, LineChart, Pie, PieChart, ResponsiveContainer, Scatter, ScatterChart, Tooltip, XAxis, YAxis } from "recharts";
const COLORS = ["#0f766e", "#f97316", "#2563eb", "#dc2626", "#7c3aed", "#059669"];
function toData(chart) { if (chart.type === "pie") return chart.labels.map((label, index) => ({ name: label, value: chart.values[index] })); return (chart.x || []).map((item, index) => ({ name: item, value: Number(chart.y?.[index] ?? 0) })); }
export function ChartPreview({ chart }) { const data = toData(chart); return <article className="chart-card"><div className="chart-header"><h4>{chart.title}</h4><span>{chart.type}</span></div><div className="chart-body"><ResponsiveContainer width="100%" height={260}>{chart.type === "line" ? <LineChart data={data}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="name" /><YAxis /><Tooltip /><Line type="monotone" dataKey="value" stroke="#2563eb" strokeWidth={3} /></LineChart> : chart.type === "scatter" ? <ScatterChart><CartesianGrid /><XAxis dataKey="name" /><YAxis dataKey="value" /><Tooltip /><Scatter data={data} fill="#0f766e" /></ScatterChart> : chart.type === "pie" ? <PieChart><Tooltip /><Pie data={data} dataKey="value" nameKey="name" outerRadius={90} label>{data.map((entry, index) => <Cell key={entry.name} fill={COLORS[index % COLORS.length]} />)}</Pie></PieChart> : <BarChart data={data}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="name" /><YAxis /><Tooltip /><Bar dataKey="value" fill="#f97316" radius={[8, 8, 0, 0]} /></BarChart>}</ResponsiveContainer></div></article>; }
